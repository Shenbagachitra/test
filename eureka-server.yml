eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:1001/eureka
welcome:
  message: "Cognizant"
management:
  server:
    port: 3001
  endpoints:
    web:
      exposure:
        includes: "*"
